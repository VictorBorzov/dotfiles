# Set prefix to space.
unbind C-b
set -g prefix C-Space

# Vim-like key bindings for pane navigation (default uses cursor keys).
unbind h
bind h select-pane -L
unbind j
bind j select-pane -D
unbind k
bind k select-pane -U
unbind l # normally used for last-window
bind l select-pane -R

# Open new/split panes with the path of the current pane.
unbind c
bind c new-window -c '#{pane_current_path}'

# Intuitive window-splitting keys.
bind | split-window -h -c '#{pane_current_path}' # normally prefix-%
bind \\ split-window -h -c '#{pane_current_path}' # normally prefix-%
bind - split-window -v -c '#{pane_current_path}' # normally prefix-"

# Enable vi for copy mode
setw -g mode-keys vi
set-window-option -g mode-keys vi

# Use v to trigger selection
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi V send-keys -X rectangle-toggle

# Use y to yank current selection
bind-key -T copy-mode-vi y send-keys -X copy-pipe-and-cancel pbcopy

# Start window and pane numbering at 1, (0 is too hard to reach).
set -g base-index 1
set -g pane-base-index 1

# Don't wait for an escape sequence after seeing C-Space.
set -s escape-time 0

# Set terminal to 256 colors
set -g default-terminal "alacritty"
set -sg terminal-overrides ",*:RGB"

set-option -g focus-events on

# set history limit to go back
set -g history-limit 4096

# set gruvbox dark style
#tm_session_name="#[fg=yellow,bold,italics]#S §"
#tm_date="#[fg=magenta]%R"
#tm_host="#[fg=yellow,bold,italics]#h"
#set -g status-style 'bg=#373b41,fg=white,bold,italics'
#set -g status-left-length 40
#set -g status-left "$tm_session_name "
#set -g status-right "$tm_host $tm_date "
#set -g status-interval 1
#set -g status-left-length 100
#set -g status-right-length 100
# Highlight active window.
#set -w -g window-status-current-style bg=red
# Show bells in window titles.
#set -g window-status-bell-style fg=yellow,bold,underscore

## set rose-pine-dawn light style
#tm_session_name="#[fg=#fae8e1,bold,italics]#S §"
#tm_date="#[fg=#fae8e1]%R"
#tm_host="#[fg=#fae8e1,bold,italics]#h"
#set -g status-style 'bg=#9893a5,fg=#ea9d34,bold,italics'
#set -g status-left-length 40
#set -g status-left "$tm_session_name "
#set -g status-right "$tm_host $tm_date "
#set -g status-interval 1
#set -g status-left-length 100
#set -g status-right-length 100
## Highlight active window.
#set -w -g window-status-current-style bg=#fae8e1,fg=#d7827e
## Show bells in window titles.
#set -g window-status-bell-style fg=#907aa9,bold,underscore

# set light style
# Basic
#set -g status-style 'bg=#9893a5,fg=white,bold,italics'
## Left part
#tm_session_name="#[fg=#ea9d34,bold,italics]#S §"
#set -g status-left-length 40
#set -g status-left "$tm_session_name "
## Right part
##tm_date="#[fg=#b4367a]%R"
##tm_host="#[fg=#b4367a,bold,italics]#h"
##set -g status-right "$tm_host $tm_date "
##set -g status-interval 1
##set -g status-left-length 100
##set -g status-right-length 100
## Highlight active window
#set -w -g window-status-current-style bg=#f2f8ff
## Show bells in window titles
##set -g window-status-bell-style fg=#907aa9,bold,underscore


# set -g status-interval 60 # Default is 15.


# Disable mouse to avoid colissions between tmux and vim
set -g mouse off

# Dynamically update iTerm tab and window titles.
set -g set-titles on

# #T      = standard window title (last command, see ~/.bash_profile)
# #h      = short hostname
# #S      = session name
# #W      = tmux window name
#
# (Would love to include #(pwd) here as well, but that would only print the
# current working directory relative to the session -- ie. always the starting
# cwd -- which is not very interesting).
# Turn on!!!!
#set -g set-titles-string "#T : #h > #S > #W"

# Turn off distracting border highlight: the background highlighting is probably
# enough. Default value of 8 comes from "pane-border-fg" in tmux's
# options-table.c.
# Turn on!!!!
# set -ga pane-active-border-style bg=default,fg=default

# But don't change tmux's own window titles.
# Turn on!!!!
# set -w -g automatic-rename off

# Automatically renumber window numbers on closing a pane (tmux >= 1.7).
# Turn on!!!!
# set -g renumber-windows on

# import rose-pine theme 
# how to use:
# set -g @rose_pine_variant 'main'
# options are 'main', 'moon' or 'dawn'
# Optional:
# set -g @rose_pine_host 'on'
# set -g @rose_pine_date_time '' # %c is a good value but 
# You can insert as an option a value that the date command accepts (man date for info)
#source-file ~/.config/tmux/themes/rose-pine-dawn.tmux

set -g @rose_pine_variant 'dawn'
set -g @rose_pine_host 'on'
set -g @rose_pine_date_time '%c' # %c is a good value but you can insert as an option a value that the date command accepts (man date for info)
run-shell ~/.config/tmux/themes/rose-pine.tmux