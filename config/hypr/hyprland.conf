monitor=eDP-1,2880x1800,0x0,2
#monitor=,highres,auto,2
exec-once=xprop -root -f _XWAYLAND_GLOBAL_OUTPUT_SCALE 32c -set _XWAYLAND_GLOBAL_OUTPUT_SCALE 2
exec-once=dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP

# See https://wiki.hyprland.org/Configuring/Keywords/ for more

# Execute your favorite apps at launch

#exec-once=swaybg -i ~/.config/background
#exec-once=hyprpaper
exec-once = swww init
exec-once = swww img $HOME/.config/background.jpg
exec-once=dunst
# exec-once=bash $HOME/.config/eww/scripts/launch_bar
# exec-once=bash $HOME/.config/eww/scripts/launch_dashboard
# exec-once=swayidle -w timeout 300 'swaylock' before-sleep 'swaylock -f'

exec-once = waybar
exec-once = blueman-applet
exec-once = nm-applet
# exec-once = libinput-gestures

exec-once = hyprctl setcursor XCursor-Pro-Light 24

# Source a file (multi-file configs)
source = ~/dotfiles/config/hypr/rose-pine-dawn.conf

# Some default env vars.
env = WLR_NO_HARDWARE_CURSORS,1
env = WLR_RENDERER_ALLOW_SOFTWARE,1
env = GDK_DPI_SCALE,0.5
env = QT_SCALE_FACTOR,1
env = GDK_SCALE,2
env = XCURSOR_SIZE,32
env = GDK_BACKEND,wayland,x11
#env GDK_BACKEND,wayland
env = QT_QPA_PLATFORM,wayland;xcb
env = SDL_VIDEODRIVER,wayland
env = CLUTTER_BACKEND,wayland
env = QT_WAYLAND_DISABLE_WINDOWDECORATION,1
env = XDG_CURRENT_DESKTOP,Hyprland
env = XDG_SESSION_TYPE,wayland
env = XDG_SESSION_DESKTOP,Hyprland
env = QT_AUTO_SCREEN_SCALE_FACTOR,2
env = GTK_THEME,Breeze
env = QT_STYLE_OVERRIDE,Breeze
env = XCURSOR_THEME,XCursor-Pro-Light
# use primarly amd gpu, if it's not available then use nvidia card
env = WLR_DRM_DEVICES,/dev/dri/card0:/dev/dri/card1


# For all categories, see https://wiki.hyprland.org/Configuring/Variables/
input {
    kb_layout = us,ru
    kb_variant =
    kb_model =
    kb_options = grp:win_space_toggle
    kb_rules =
    follow_mouse = 1

    touchpad {
        natural_scroll = yes
    }

    sensitivity = 0.3 # -1.0 - 1.0, 0 means no modification.
}

general {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    gaps_out = 20 #3 # 10
    border_size = 3
    col.active_border = $muted # rgba(ffffffee)
    col.inactive_border = $overlay # rgba(ffffffee)

    layout = dwindle
}

decoration {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    rounding = 10 # 6 or 3
    # multisample_edges=1 # enable antialiasing for rounded corners

    active_opacity = 1.0 # 0.95
    inactive_opacity = 0.9 # 0.95
    fullscreen_opacity = 1.0
    blur {
        enabled = true
        size = 3
        passes = 1
        new_optimizations = on
        blurls = waybar
    }

    drop_shadow = true
    shadow_range = 30
    shadow_render_power = 3
    col.shadow = 0x66000000


    # dim_inactive = true
    # dim_strength = 0.1
    # drop_shadow = yes
    # shadow_range = 4
    # shadow_render_power = 3
    # col.shadow = rgba(1a1a1aee)
}

animations {
    enabled = true
    bezier = myBezier, 0.05, 0.9, 0.1, 1.05
    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
}

#animations {
#    enabled = yes
#
#    # Some default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more
#
#    # bezier = myBezier, 0.05, 0.9, 0.1, 1.05
#
#    # animation = windows, 1, 7, myBezier
#    # animation = windowsOut, 1, 7, default, popin 80%
#    # animation = border, 1, 10, default
#    # animation = borderangle, 1, 8, default
#    # animation = fade, 1, 7, default
#    # animation = workspaces, 1, 6, default
#
#    animation=windows,1,2,default
#    animation=border,1,10,default
#    animation=fade,0,5,default
#    animation=workspaces,1,4,default
#}
dwindle {
    # See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
    pseudotile = true # no # master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    # force_split = 2
    preserve_split = true # you probably want this
}

master {
    # See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
    new_is_master = true
    orientation = left
}

gestures {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    workspace_swipe = off
}

xwayland {
    use_nearest_neighbor = true
    force_zero_scaling = true
}

# Example windowrule v1
# windowrule = float, ^(kitty)$
# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$
# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
#windowrulev2 = tile,class:(Librewolf),title:(.*)
#windowrulev2 = opacity 0.9 override 0.9 override,class:(wezterm),title:(.*)

########## keybinds ##########

bind=SUPER,Return,exec,kitty
bind=SUPER,t,exec,thunar

bind=SUPER,Tab,cyclenext
bind=SUPER,Q,killactive,
# bind = SUPER, k, pseudo, # dwindle
# bind = SUPER, j, togglesplit, # dwindle

bind=SUPER,r,exec,bash ~/.config/rofi/application-launcher-wayland.sh
bind=SUPER,p,exec,bash ~/.config/rofi/powermenu-wayland.sh
#bind=SUPER,n,exec,bash ~/.config/wofi/network/network.sh
bind=SUPER,d,exec,bash ~/.config/eww/scripts/launch_dashboard
bind=SUPER,b,exec,swaybg -i ~/.config/hypr/background
bind=SUPER,f,fullscreen
bind=SUPERSHIFT,l,exec,bash ~/.config/scripts/swaylock-and-suspend.sh
# bind=SUPERSHIFT,d,exec,bash if [[ "$DARK_MODE" == "True" ]]; then ~/dotfiles/scripts/light-mode.sh; else ~/dotfiles/scripts/dark-mode.sh; fi

# swap windows
bind=SUPER,left,movewindow,l
bind=SUPER,down,movewindow,d
bind=SUPER,up,movewindow,u
bind=SUPER,right,movewindow,r

# Move focus with mainMod + arrow keys
bind = SUPER, h, movefocus, l
bind = SUPER, l, movefocus, r
bind = SUPER, k, movefocus, u
bind = SUPER, j, movefocus, d

# change workspace
bind=SUPER,1,workspace,1
bind=SUPER,2,workspace,2
bind=SUPER,3,workspace,3
bind=SUPER,4,workspace,4
bind=SUPER,5,workspace,5
bind=SUPER,6,workspace,6
bind=SUPER,7,workspace,7
bind=SUPER,8,workspace,8
bind=SUPER,9,workspace,9
bind=SUPER,0,workspace,10

# move window to given workspace
bind=SUPERSHIFT,1,movetoworkspace,1
bind=SUPERSHIFT,2,movetoworkspace,2
bind=SUPERSHIFT,3,movetoworkspace,3
bind=SUPERSHIFT,4,movetoworkspace,4
bind=SUPERSHIFT,5,movetoworkspace,5
bind=SUPERSHIFT,6,movetoworkspace,6
bind=SUPERSHIFT,7,movetoworkspace,7
bind=SUPERSHIFT,8,movetoworkspace,8
bind=SUPERSHIFT,9,movetoworkspace,9
bind=SUPERSHIFT,0,movetoworkspace,10

# toggle float on active window
bind=SUPER,w,togglefloating
bind = SUPER, y, togglesplit

bind=SUPERSHIFT,left,resizeactive,-40 0
bind=SUPERSHIFT,down,resizeactive,0 40
bind=SUPERSHIFT,up,resizeactive,0 -40
bind=SUPERSHIFT,right,resizeactive,40 0

#move and resize with SUPER and mouse
bindm=SUPER,mouse:272,movewindow
bindm=SUPERSHIFT,mouse:272,resizewindow


##### screenshot #####
bind=,F11,exec, bash ~/.config/hypr/screenshot.sh



##### toggle fullscreen #####


# See https://wiki.hyprland.org/Configuring/Keywords/ for more
$mainMod = SUPER

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
# bind = $mainMod, Q, exec, wezterm
# bind = $mainMod, C, killactive, 
# bind = $mainMod, M, exit, 
# bind = $mainMod, E, exec, dolphin
# bind = $mainMod, V, togglefloating, 
# bind = $mainMod, R, exec, wofi --show drun
# bind = $mainMod, k, pseudo, # dwindle
# bind = $mainMod, j, togglesplit, # dwindle
# bind = $mainMod, F, fullscreen

# # Move focus with mainMod + arrow keys
# bind = $mainMod, left, movefocus, l
# bind = $mainMod, right, movefocus, r
# bind = $mainMod, up, movefocus, u
# bind = $mainMod, down, movefocus, d

# # Switch workspaces with mainMod + [0-9]
# bind = $mainMod, 1, workspace, 1
# bind = $mainMod, 2, workspace, 2
# bind = $mainMod, 3, workspace, 3
# bind = $mainMod, 4, workspace, 4
# bind = $mainMod, 5, workspace, 5
# bind = $mainMod, 6, workspace, 6
# bind = $mainMod, 7, workspace, 7
# bind = $mainMod, 8, workspace, 8
# bind = $mainMod, 9, workspace, 9
# bind = $mainMod, 0, workspace, 10

# # Move active window to a workspace with mainMod + SHIFT + [0-9]
# bind = $mainMod SHIFT, 1, movetoworkspace, 1
# bind = $mainMod SHIFT, 2, movetoworkspace, 2
# bind = $mainMod SHIFT, 3, movetoworkspace, 3
# bind = $mainMod SHIFT, 4, movetoworkspace, 4
# bind = $mainMod SHIFT, 5, movetoworkspace, 5
# bind = $mainMod SHIFT, 6, movetoworkspace, 6
# bind = $mainMod SHIFT, 7, movetoworkspace, 7
# bind = $mainMod SHIFT, 8, movetoworkspace, 8
# bind = $mainMod SHIFT, 9, movetoworkspace, 9
# bind = $mainMod SHIFT, 0, movetoworkspace, 10

# # Scroll through existing workspaces with mainMod + scroll
# bind = $mainMod, mouse_down, workspace, e+1
# bind = $mainMod, mouse_up, workspace, e-1

# # Move/resize windows with mainMod + LMB/RMB and dragging
# bindm = $mainMod, mouse:272, movewindow
# bindm = $mainMod, mouse:273, resizewindow

# Volume and Brightness
bind = , XF86AudioRaiseVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+
bind = , XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bind = , XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bind = , XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bind = , XF86MonBrightnessDown, exec, brightnessctl set 5%-
bind = , XF86MonBrightnessUp, exec, brightnessctl set 5%+

# Eco mode
bind = WIN, F1, exec, ~/.config/hypr/ecomode.sh
gestures {
  workspace_swipe = true
  #workspace_swipe_fingers = 3
  #workspace_swipe_distance = 300
  workspace_swipe_invert = true
  workspace_swipe_min_speed_to_force = 10
  workspace_swipe_cancel_ratio = 0.85
}

misc {
    disable_hyprland_logo = true
    disable_splash_rendering = true
}
